node_modules
dist
package-lock.json
.env
.env.local
.DS_Store
*.log
.vscode
.idea

# Azure backend lives separately at ~/Desktop/azure — never tracked in the frontend repo
/azure/
.claude/

# SQL migrations are kept outside the repo; share them out-of-band
/sql/
*.sql
